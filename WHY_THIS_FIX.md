Fragments auth and Hope Task auth are separate credentials (`FRAGMENTS_AUTH_PASSPHRASE` vs `APP_PASSPHRASE`). The old fragment-level live-sync widget crossed origins and therefore asked for a Hope Task session inside Fragments. That widget is not required for canonical capture routing, which already has a server-to-server secret-backed path. This release uses that existing path automatically and removes the extra human authentication step from the capture workflow.